# Ambient Scratchpad - Dependencies

# Core requirements
rumps>=0.4.0              # macOS menubar app framework
google-genai>=1.0.0       # Google Gemini API client for classification & embeddings
python-dotenv>=1.0.0      # Environment variable management
pathway>=0.8.0            # Real-time streaming pipeline (main feature)

# macOS native UI (PyObjC)
pyobjc>=10.0              # Python-Objective-C bridge for native windows

# Optional but recommended
numpy>=1.24.0             # Fast vector operations for embeddings
requests>=2.31.0          # HTTP library for URL fetching
beautifulsoup4>=4.12.0    # HTML parsing for scraped content

# Development/Testing
pytest>=7.0.0             # Testing framework








